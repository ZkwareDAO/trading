# =============================================================================
# 测试用配置 - 旧格式（main.yaml 风格）
# =============================================================================
# 用于 backtest/tests/test_batch_runner.py
# 此格式已废弃，仅用于兼容测试
# =============================================================================

start: "20260101"
end: "20260331"
data_dir: "./data/strategies"
output_dir: "./backtest_output"
max_workers: 4
log_level: "INFO"

strategies:
  - name: cta_ict_v2
    symbols: [BTCUSDT]
